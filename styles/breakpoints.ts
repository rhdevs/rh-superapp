const size = {
 sm: '640px',
 md: '768px',
 lg: '1280px',
}
const device = {
 sm: `(min-width: ${size.sm})`,
 md: `(min-width: ${size.md})`,
 lg: `(min-width: ${size.lg})`
}
export default {size, device}